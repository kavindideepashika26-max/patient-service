version: "3.8"

services:
  patient:
    image: patient
    container_name: patient
    ports:
      - "8082:8081"
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://host.docker.internal:3306/patientdb
      SPRING_DATASOURCE_USERNAME: root
      
      SPRING_DATASOURCE_PASSWORD: 

